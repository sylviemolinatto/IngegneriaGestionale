package it.polito.tdp.borders.model;

import java.util.Set;

import org.jgrapht.Graph;
import org.jgrapht.graph.DefaultEdge;

public class TestModel {

	public static void main(String[] args) {

		/* Model model = new Model();

		System.out.println("TestModel -- TODO");
		
		Graph grafo = model.creaGrafo(1999);
		System.out.println(grafo.vertexSet().size());
		System.out.println(grafo.edgeSet().size());
		Set<Country> vertex = grafo.vertexSet();
		

		for(Country c : vertex) {
			System.out.println("Il vertice "+c.getcCode()+" ha esattamente "+grafo.degreeOf(c)+" paesi confinanti");
		}
		
		System.out.println(model.visitaGrafo(new Country(100,"COL","Colombia"))); */
		
//		System.out.println("Creo il grafo relativo al 2000");
//		model.createGraph(2000);
		
//		List<Country> countries = model.getCountries();
//		System.out.format("Trovate %d nazioni\n", countries.size());

//		System.out.format("Numero componenti connesse: %d\n", model.getNumberOfConnectedComponents());
		
//		Map<Country, Integer> stats = model.getCountryCounts();
//		for (Country country : stats.keySet())
//			System.out.format("%s %d\n", country, stats.get(country));		
		
	}

}
